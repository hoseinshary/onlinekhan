import { displayName } from 'plugins/vuelidate';

export default {
  accessObj: {
    ModuleId: {
      displayName: displayName('منو')
    },
    ControllerId: {
      displayName: displayName('زیر منو')
    }
  }
};
